interface:
  display_name: "Web Security Review"
  short_description: "Audit web apps, AI agents, and MCP flows"
  default_prompt: "Use $web-security-review to perform an evidence-backed security audit of this application, AI agent, or MCP workflow."

policy:
  allow_implicit_invocation: true
